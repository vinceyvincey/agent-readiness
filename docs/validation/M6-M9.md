# M6-M9 - Check fixes, deepening, monorepo scope, agentic remediation

Date: 2026-08-26. Milestones M6-M9 closed.

## M6 - Check-correctness & git-aware foundations

### Bugs fixed
- **P6.2 (no-op secret scan):** was scanning `.gitignore` + empty object; now scans git-tracked files (up to 200, binary-filtered) for 8 secret patterns (AWS keys, GitHub PATs, OpenAI keys, Slack tokens, Google API keys, private keys).
- **P5.3 (broken type-check):** `read(r+'')` coerced Repo to `[object Object]`; now reads `pyproject.toml`/`setup.cfg` for mypy/pyright, detects `go.mod`/`Cargo.toml` for Go/Rust.
- **P6.3 (filesystem .env):** was `!has(r,'.env')` (penalizes git-ignored .env); now uses `git ls-files` to check for tracked .env files, excluding .env.example/.env.sample.

### Added
- Git provenance in report.run: `commitHash`, `branch`, `hasLocalChanges`, `hasNonRemoteCommits`.
- test/checks.test.ts: 9 assertions covering planted secrets, pyproject/go type-check, git-ignored vs tracked .env.

### Results
- Self-score: L0/30 (unchanged — no scoring regression from bug fixes).
- P6.2 evidence: "scanned 71 tracked files, no secret patterns" (real scan, not no-op).

## M7 - Anti-gaming & check deepening

### Anti-gaming guards
- P0.1: README anti-stub (strip frontmatter + HTML comments, require >=2 content lines + >200 bytes).
- P1.2: AGENTS.md command verification (extract backtick/fenced commands, verify >=1 matches real script/target).
- P6.1: .gitignore anti-one-liner (>=3 distinct non-comment patterns).
- P2.4: Coverage threshold=0 guard (decorative coverage fails).
- P4.2: CI-runs-tests verification (strip echo stubs, look for real npm test / pytest / go test / cargo test invocations).

### Deepened checks
- P6.4: Optional gitleaks/trufflehog on PATH; fallback to regex scan.
- P2.6: Fast/smoke test path detection (test:fast script, Makefile test-fast, vitest/jest testPathIgnorePatterns).
- P1 expanded: P1.6 (hooks), P1.7 (custom droids), P1.8 (connectors) — .factory/ + .pi/ paths.

### Difficulty axis
- Each check tagged `basic` / `intermediate` / `advanced`.
- Punchlist sorted by severity (high→med→low) then difficulty (basic first).

### Results
- Rubric bumped to 0.2.0.
- Corpus recalibrated: high L2/81 (was L2/79.3), low L0/11, this repo L0/30.
- E1 harness: H3 stability ✓, H5 mandatory gates ✓, H1 separation gap=70 ✓, H8 scaffold-effect ✓.
- test/deepening.test.ts: 16 assertions, all pass.

## M8 - Monorepo / application scope

### Added
- src/discover.ts: `discoverApps(root)` detects monorepo structures (package.json workspaces, pnpm-workspace, turbo/nx/lerna, apps/*/packages/ globs, go.work, Cargo workspace, Python packages/).
- Pillars tagged `repo` or `app` scope. App-scoped: P2, P3, P5, P7, P9. Repo-scoped: P0, P1, P4, P6, P8.
- App-scoped pillars run per discovered app; aggregate score = sum(passed)/sum(total).
- Report includes `apps` map + per-app `perApp` breakdown; findings carry `app` field.

### Results
- Rubric bumped to 0.3.0.
- Single-app repos: no regression (1 app at root, no perApp, identical scores).
- Monorepo: per-app scoring verified with synthetic 2-package and 3-package fixtures.
- test/monorepo.test.ts: 17 assertions, all pass.

## M9 - Agentic remediation

### Added
- `agentPromptFor(report)`: builds a grounded remediation prompt from the punchlist (severity+difficulty sorted, evidence, app context for monorepos, safety instructions).
- Extension `/readiness-fix`: rewritten to return the agent prompt for a delegated agent session.
- CLI `--fix --agent`: shells out to `pi -p <prompt>`; prints prompt if pi not on PATH; re-runs readiness post-fix to show delta.
- Static `--fix` (no `--agent`): unchanged for headless/CI use.

### Results
- test/fix.test.ts: 13 assertions (prompt contains failing checks, safety section, monorepo awareness, static draft no regression).
- All 6 test suites pass; E1 harness all pass.

## Cumulative test summary

| Suite | Assertions | Status |
|---|---|---|
| test/engine.test.ts | 13 | ALL PASS |
| test/history.test.ts | 8 | ALL PASS |
| test/checks.test.ts | 9 | ALL PASS |
| test/deepening.test.ts | 16 | ALL PASS |
| test/monorepo.test.ts | 17 | ALL PASS |
| test/fix.test.ts | 13 | ALL PASS |
| validation/run-harness.ts (E1) | 8 | ALL PASS |

## Hypothesis status
- H3 (stability): ✓ passing — all repos identical across 3 repeat runs, rubric 0.3.0.
- H5 (mandatory gates): ✓ passing — no repo at L3+ with P2/P6 below gate.
- H8 (scaffold-effect): ✓ passing — config_hash consistent, engine is caller/model-independent.
- H1 (E1 separation): ✓ passing — high/low gap=70 (>=40 needed).
